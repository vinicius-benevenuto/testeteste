
Quero que você atue como um engenheiro de software sênior, especialista, componentes de tabela, renderização condicional, fluxo de dados e depuração de interfaces. Sua missão é identificar exatamente quais erros impedem que a tabela seja exibida na aba “Tabela”.

Com base nesses códigos, você irá:

1. Ler e interpretar o código profundamente.  
2. Mapear toda a lógica de carregamento, transformação e exibição dos dados.  
3. Identificar com precisão cirúrgica todos os pontos que podem impedir a renderização da tabela, tais como:
   - Estado mal inicializado.
   - Dados não carregados ou carregados no formato errado.
   - Condição de renderização que esconde a tabela.
   - Hooks configurados incorretamente.
   - Erros de importação do componente de tabela.
   - Componente de tabela recebendo props vazias.
   - Retornos vazios no JSX.
   - Renderização antes dos dados estarem disponíveis.
   - Variáveis referenciadas incorretamente.
   - Estrutura da árvore JSX inválida.

4. Listar **todos os erros encontrados** — tanto erros reais quanto erros potenciais.  
5. Indicar a causa raiz.  
6. Aguardar meu comando para só então sugerir a correção.

Regras:
- Não execute nada.
- Não modifique nada.
- Não sugira nada até eu pedir.
- Não resuma.
- Apenas leia e entenda profundamente.
- Ao finalizar a leitura do código, responda apenas:

**“Lido. Pronto para diagnosticar.”**
